'use client'

import EventDashboardClient from '@/components/EventDashboardClient'

/** คง `/event` ไว้ให้ลิงก์เดิม — เนื้อหาเดียวกับ `/dashboard` */
export default function EventDashboardPage() {
  return <EventDashboardClient />
}
